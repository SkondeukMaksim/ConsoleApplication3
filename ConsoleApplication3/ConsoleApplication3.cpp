#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;

int main() {
    double R1, R2, R3, R0; 

    /*cout << "Enter R1: "; 
    cin >> R1; 
    cout << "Enter R2: "; 
    cin >> R2; 
    cout << "Enter R3: "; 
    cin >> R3; 

    R0 = 1 / (1 / R1 + 1 / R2 + 1 / R3); 
    cout << "R0 = " << R0 << endl; 

    return 0;*/ 

    /*double L, R, S;

    cout << "Enter L: "; 
    cin >> L; 

    R = L / (2 * 3.14); 
    S = 3.14 * pow(R, 2);
    cout << "S = " << S << endl; 
    return 0; */

    /*double v, t, a, S; 

    cout << "Enter v: "; 
    cin >> v; 
    cout << "Enter t: "; 
    cin >> t; 
    cout << "Enter a: "; 
    cin >> a; 

    S = v * t + (a * pow(t, 2)) / 2; 
    cout << "S = " << S << endl; 

    return 0; */
}